# NewsFlow RSS Reader - Environment Configuration Example
# Copy this file to .env and configure as needed

# API Configuration
# Leave empty for local development (uses /api relative path)
# For production, set to your deployed API base URL (e.g., https://your-api.vercel.app)
# Note: /api path will be appended automatically if not already included
NEXT_PUBLIC_API_URL=
